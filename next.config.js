/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/Technologymonastery.org',
  images: {
    unoptimized: true,
  },
  trailingSlash: true,
};

module.exports = nextConfig;
